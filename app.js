'use strict';

const read = require('./lib/read');
const create_new = require('./lib/create_new');

read('./img/palette-bitmap.bmp'); //running my main app, which imports in the necessary modules to run the program on the original .bmp
